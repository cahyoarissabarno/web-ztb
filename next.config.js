/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'dummyimage.com', 'lzd-img-global.slatic.net']
  }
}

module.exports = nextConfig
